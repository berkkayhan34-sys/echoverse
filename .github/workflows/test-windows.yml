# SPDX-FileCopyrightText: 2026 EchoVerse contributors
# SPDX-License-Identifier: GPL-3.0-only

name: Test Windows Build

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  windows-test:
    name: Windows Test Build
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Read package version
        id: version
        shell: pwsh
        run: |
          $version = (Get-Content "VERSION" -Raw).Trim()
          $desktopVersion = (Get-Content "apps/desktop/package.json" -Raw | ConvertFrom-Json).version
          if ($desktopVersion -ne $version) { throw "apps/desktop/package.json version mismatch: canonical=$version package=$desktopVersion" }
          if (-not $version) { throw "apps/desktop/package.json version missing" }
          "version=$version" >> $env:GITHUB_OUTPUT
          Write-Host "Testing EchoVerse $version"

      - name: Install dependencies
        working-directory: .
        run: npm ci

      - name: Build Windows
        working-directory: apps/desktop
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
        run: npm run dist:win

      - name: Verify branding and sound assets
        shell: pwsh
        run: |
          $required = @(
            "apps/desktop/assets/echoverse.ico",
            "apps/desktop/assets/echoverse-icon.png",
            "apps/desktop/assets/echoverse-splash.png",
            "apps/desktop/public/echoverse-icon.png",
            "apps/desktop/public/echoverse-wordmark.png",
            "apps/desktop/public/sounds/voice-join.wav",
            "apps/desktop/public/sounds/voice-leave.wav",
            "apps/desktop/public/sounds/message.wav",
            "apps/desktop/public/sounds/mic-toggle.wav",
            "apps/desktop/public/sounds/incoming-call.wav",
            "apps/desktop/dist/echoverse-icon.png",
            "apps/desktop/dist/echoverse-wordmark.png",
            "apps/desktop/dist/sounds/voice-join.wav",
            "apps/desktop/dist/sounds/voice-leave.wav",
            "apps/desktop/dist/sounds/message.wav",
            "apps/desktop/dist/sounds/mic-toggle.wav",
            "apps/desktop/dist/sounds/incoming-call.wav"
          )

          foreach ($file in $required) {
            if (!(Test-Path $file)) { throw "Required branding/sound asset missing: $file" }
          }

          $pkg = Get-Content "apps/desktop/package.json" -Raw
          if ($pkg -notmatch '"to":\s*"branding"') { throw "Packaged branding resources are not configured" }

          $app = Get-Content "apps/desktop/src/App.tsx" -Raw
          if ($app -match 'src="/echoverse-wordmark.png"') { throw "Absolute wordmark URL would break under file:// packaging" }
          if ($app -match '"/sounds/') { throw "Absolute sound URL would break under file:// packaging" }

          $main = Get-Content "apps/desktop/electron/main.cjs" -Raw
          if ($main -notmatch 'process\.resourcesPath') { throw "Packaged Electron branding path missing" }

      - name: Verify v1.7.4 voice assets
        shell: pwsh
        run: |
          $files = @(
            "apps/desktop/public/sounds/outgoing-call.wav",
            "apps/desktop/public/sounds/call-connected.wav",
            "apps/desktop/public/sounds/call-ended.wav",
            "apps/desktop/public/sounds/deafen-toggle.wav",
            "apps/desktop/public/sounds/screen-share-toggle.wav",
            "apps/desktop/public/sounds/mention.wav"
          )
          foreach ($f in $files) {
            if (!(Test-Path $f)) { throw "Missing v1.7.4 sound asset: $f" }
          }

      - name: Verify updater metadata
        shell: pwsh
        run: |
          $version = "${{ steps.version.outputs.version }}"
          $installer = "apps/desktop/release/EchoVerse-Setup-$version.exe"
          $blockmap = "$installer.blockmap"
          $latest = "apps/desktop/release/latest.yml"

          if (!(Test-Path $installer)) { throw "Installer missing: $installer" }
          if (!(Test-Path $blockmap)) { throw "Blockmap missing: $blockmap" }
          if (!(Test-Path $latest)) { throw "latest.yml missing" }

          $content = Get-Content $latest -Raw
          Write-Host $content

          $escaped = [regex]::Escape($version)
          if ($content -notmatch "version:\s*$escaped") { throw "Version mismatch" }
          if ($content -notmatch "EchoVerse-Setup-$escaped\.exe") { throw "Installer filename mismatch" }

      - uses: actions/upload-artifact@v4
        with:
          name: EchoVerse-Windows-Test
          path: |
            apps/desktop/release/EchoVerse-Setup-*.exe
            apps/desktop/release/EchoVerse-Setup-*.exe.blockmap
            apps/desktop/release/latest.yml
          if-no-files-found: error
