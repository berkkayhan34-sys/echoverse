# EchoVerse v1.7.1 — Branding & UX Polish

- Uses the supplied EchoVerse square artwork for Windows EXE/installer/taskbar and tray branding.
- Uses the supplied EchoVerse wordmark inside the application.
- Uses the supplied galaxy EchoVerse artwork as the native startup splash.
- Adds a Windows tray icon and compact tray menu.
- Adds micro-interaction polish, scrollbar/input/modal/loading/empty-state styling.
- Keeps the v1.7.0 video, screen-share and voice-sync behavior intact.
- Sound-effect slots remain compatible with the existing lobby join/leave audio; additional user-supplied sounds can be dropped in next without redesigning the UI.

## Final sound pack
- voice-join.wav
- voice-leave.wav
- message.wav
- mic-toggle.wav
- incoming-call.wav
- Assets are bundled under desktop/public/sounds.
- Existing voice join/leave asset references are redirected to the supplied sounds.
- Mic/message/call hooks are wired where the corresponding existing event/function exists, without inventing new server events.

## Startup hotfix
- Removed the duplicate `let tray = null` declaration that caused Electron main-process startup failure.
- Consolidated duplicate `createTray()` functions into one branded implementation.
- Splash screen now starts before the main window and closes after the renderer is ready.
- Main window stays hidden while the splash is visible.
