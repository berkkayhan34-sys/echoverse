# SPDX-FileCopyrightText: 2026 EchoVerse contributors
# SPDX-License-Identifier: GPL-3.0-only

PORT=3001
NODE_ENV=development
JWT_SECRET=CHANGE_THIS_TO_A_LONG_RANDOM_SECRET
DATABASE_URL=
SQLITE_PATH=
# Use false for the local PostgreSQL instance; keep true for a TLS-enabled hosted database.
DATABASE_SSL=false
# Render's managed PostgreSQL uses a self-signed CA; set false only for that hosted service.
DATABASE_SSL_REJECT_UNAUTHORIZED=true
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
TRUST_PROXY=false
WEB_COOKIE_SECURE=false
WEB_COOKIE_SAMESITE=lax
SESSION_ACCESS_TTL_SECONDS=900
SESSION_REFRESH_TTL_SECONDS=604800
