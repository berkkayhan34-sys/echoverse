# v1.6.5 Chat Hotfix

DM ekranındaki siyah ekran düzeltildi.

Sebep:
DM mesaj listesi `React.Fragment` kullanıyordu fakat App.tsx içinde `React`
runtime import edilmemişti. Build bunu yakalamıyordu; mesaj bulunan bir DM
render edildiğinde runtime'da `React is not defined` oluşabiliyordu.

Düzeltme hem desktop hem web App.tsx'e uygulandı.
