<!--
SPDX-FileCopyrightText: 2026 EchoVerse contributors
SPDX-License-Identifier: GPL-3.0-only
-->

# Evidence: `BUG-002`

```yaml
id: BUG-002
status: in_progress
date: 2026-08-30
revision: 52a3a40 (main, tagged v1.9.4)
```

## Scope

- Non-owner private guilds expose a visible, keyboard- and touch-accessible
  leave menu; the public main guild and owners remain protected by the
  existing server authorization.
- Guild voice peer setup is serialized per socket pair, uses a deterministic
  initiator to avoid offer glare, queues ICE candidates that arrive before the
  remote description, and acquires the microphone during the explicit join
  action.
- The DM composer keeps the message field flexible and the send action compact
  across desktop and narrow layouts.

## Validation

| Check                                                                     | Result      | Evidence                                                                                                                                                          |
| ------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `npm test -- --run project/packages/shared-ui/src/index.test.ts`          | pass        | Shared leave-menu and DM composer component contracts passed (22 tests).                                                                                          |
| `npm test -- --run project/apps/server/src/integration.test.ts`           | pass        | Guild membership and authorized WebRTC relay coverage passed (23 tests).                                                                                          |
| `npm test -- --run project/apps/web/src/features/direct-messages.test.ts` | pass        | Direct-message feature transport test passed.                                                                                                                     |
| `npm test`                                                                | pass        | Full Vitest suite passed (23 files, 116 tests).                                                                                                                   |
| `npm run typecheck`                                                       | pass        | All workspace packages typechecked successfully.                                                                                                                  |
| `npm run lint` and Prettier check                                         | pass        | ESLint and formatting checks completed without findings.                                                                                                          |
| `npm run build`                                                           | pass        | Web, desktop, server, and shared packages built successfully.                                                                                                     |
| `npm audit --audit-level=high`                                            | pass        | No high-or-critical dependency vulnerabilities reported.                                                                                                          |
| `npm run reuse:check`                                                     | unavailable | The repository requires the `reuse` CLI, which is not installed on this machine.                                                                                  |
| `npm run secret-scan`                                                     | unavailable | The repository requires the `gitleaks` CLI, which is not installed on this machine.                                                                               |
| Integrated browser                                                        | partial     | Local landing surface loaded successfully. Authenticated DM/voice interaction remains pending because no credential-entry confirmation was provided in this turn. |
| Release/deployment                                                        | pass        | Main commit `52a3a40` was tagged `v1.9.4`; [release workflow 33324416819](https://github.com/berkkayhan34-sys/echoverse/actions/runs/33324416819) published all 12 Windows/macOS artifacts; [Render health](https://echoverse-c3d5.onrender.com/health) returned HTTP 200 with version `1.9.4`. |

## Security and recovery notes

No server authorization boundary was weakened. Guild signaling remains limited
to authenticated sockets in the same active voice room, and the leave action
still uses the server-side membership and owner checks. The client changes are
recoverable on the work branch; no persisted data or production state was
modified.
