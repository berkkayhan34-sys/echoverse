<!--
SPDX-FileCopyrightText: 2026 EchoVerse contributors
SPDX-License-Identifier: GPL-3.0-only
-->

# BUG-003 evidence

```yaml
id: BUG-003
status: complete
date: 2026-08-30
revision: working-tree
```

Status: complete (implementation, automated verification, and authenticated
rendered browser verification passed).

Implemented surfaces:

- `guild:delete` is owner-only, protects `echoverse`, and fails closed unless
  all other members are `test@test.com` or `test2@test2.com`.
- Persisted deletion removes the guild row and relies on the existing foreign
  key cascades for guild-owned records; in-memory maps and connected rooms are
  cleared as part of the same operation.
- Desktop and responsive shared UI expose owner Delete and member Leave actions,
  plus New server / Join with server code actions.

Focused checks:

- `npm run typecheck`
- `npm test -- --run project/packages/shared-ui/src/index.test.ts project/apps/server/src/features/guilds/service.test.ts`
- `npm run format:check`
- `npm run lint`
- `npm run localization:check`
- `npm run version:check`
- `npm audit --audit-level=high`
- `npm run reuse:check` (REUSE 6.2.0)
- `npm run secret-scan` (Gitleaks 8.30.1; 124 commits scanned, no leaks found)
- `npm run build`
- `npm test -- --run project/packages/contracts/src/index.test.ts project/packages/shared-ui/src/index.test.ts project/apps/server/src/features/guilds/service.test.ts project/apps/server/src/integration.test.ts`

Authenticated rendered verification (2026-08-30, local server):

- Started the local server on port `3001` and confirmed `GET /health` returned
  HTTP 200 with the memory database runtime.
- Logged in through the web UI with the owner test account, created a private
  `BUG003 Test` guild, and verified the authenticated workspace rendered with
  its text and voice channels.
- Opened the left-rail three-dot menu and verified the owner-only Delete action
  (`Sunucuyu sil`) was present; the guild header also exposed the same action.
- Opened the plus menu and verified `Sunucuya katıl` rendered a join-code
  dialog. Logged out, authenticated as the second test account, entered the
  invite code, and verified the private guild appeared and opened for that
  member.
- On the member session, verified the three-dot menu exposed `Sunucudan çık`
  and did not expose deletion. Re-authenticated as the owner and deleted the
  test guild; the confirmation completed and the server rail immediately
  rendered only the protected `EchoVerse` main guild.
- Captured rendered screenshots before and after the flow; no clipping,
  overlay, or stale deleted-guild entry was observed in the tested desktop
  viewport.

Environment note: this sandbox's Node runtime raises
`uv_os_get_passwd returned ENOMEM` when `tsx` discovers its temporary
directory. The verification used a temporary, ignored local preload that
provided the username probe only for the test runner; no application or
production startup code was changed.
